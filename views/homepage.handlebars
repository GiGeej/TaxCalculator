{{#if logged_in}}
<h5 class="card-header">View Your Profile</h5>
  <div class="card-body">
   <button type="button" class="btn btn-primary" disabled>
      View Profile
    </button>
{{else}}
{{> login}}
{{/if}}